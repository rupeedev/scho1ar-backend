# Scho1ar Backend Environment Variables

# Server Configuration
HOST=0.0.0.0
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL=postgres://postgres:password@localhost:5432/scho1ar

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Clerk Authentication (Required)
# Your Clerk instance URL (e.g., https://your-app.clerk.accounts.dev)
CLERK_ISSUER=https://your-app.clerk.accounts.dev

# Optional: Explicitly set the JWKS URL (defaults to {CLERK_ISSUER}/.well-known/jwks.json)
# CLERK_JWKS_URL=https://your-app.clerk.accounts.dev/.well-known/jwks.json

# Optional: JWT audience validation (usually your frontend URL or Clerk app ID)
# CLERK_AUDIENCE=
